---
title:
  en: "Pathfinder 4.4.0 is live: it can now run several AI agents at once"
  es: "Pathfinder 4.4.0 ya está disponible: ahora puede ejecutar varios agentes de IA a la vez"
  fr: "Pathfinder 4.4.0 est disponible : il peut maintenant faire tourner plusieurs agents IA en parallèle"
summary:
  en: My workflow kit used to do one ticket at a time, on purpose. The new orchestrator mode runs several agents in parallel, each in its own copy of the repository, and I still own every decision that matters.
  es: Mi kit de flujo de trabajo hacía un ticket a la vez, a propósito. El nuevo modo orquestador ejecuta varios agentes en paralelo, cada uno en su propia copia del repositorio, y yo sigo siendo dueño de cada decisión que importa.
  fr: Mon kit de workflow traitait un ticket à la fois, délibérément. Le nouveau mode orchestrateur fait tourner plusieurs agents en parallèle, chacun dans sa propre copie du dépôt, et je garde toutes les décisions qui comptent.
shareCaption:
  en: "Pathfinder 4.4.0 is live. 🧭 It can now run several AI agents at once, each in its own copy of the repo. Both agents died mid-run during testing and nothing was lost."
  es: "Pathfinder 4.4.0 ya está disponible. 🧭 Ahora ejecuta varios agentes de IA a la vez, cada uno en su propia copia del repo. En las pruebas murieron los dos agentes a mitad de ejecución y no se perdió nada."
  fr: "Pathfinder 4.4.0 est disponible. 🧭 Il fait tourner plusieurs agents IA en parallèle, chacun dans sa propre copie du dépôt. Pendant les tests, les deux agents sont morts en cours de route et rien n'a été perdu."
date: "2026-09-19"
tags: ["ai", "engineering", "pathfinder", "agents", "open-source"]
draft: false
---

<!-- locale:en -->

Pathfinder is the workflow kit I use to build software with AI agents. Until now it did one thing at a time, on purpose. Version 4.4.0 is live, and it can run several agents at once without giving up the part that makes it safe.

Here is what that actually means.

## The slow part was never the thinking

The kit exists because an agent can write code far faster than anyone can responsibly approve it. So I put a human at every point where a decision had consequences: approve the plan, accept the work, merge it, release it.

That worked. It also got slow, for a slightly silly reason.

Two tickets touching completely unrelated parts of a codebase would still run one after the other, because there was one session and I was sitting in it. The queue was not waiting for me to think. It was waiting for me to be available. That is not a human in the loop, it is a human as a scheduler.

## The new mode

A project now says how it wants to run, in one line of one file:

```text
<!-- pathfinder:execution-mode orchestrator -->
```

`human-in-the-loop` is the old behaviour: one ticket, one session, you drive. `orchestrator` lets Pathfinder work on several tickets at the same time, give each one its own agent, and interrupt you only when something genuinely needs a person.

A project with no mode file at all keeps the old behaviour. There is nothing to migrate.

```bash
npx create-pathfinder@4.4.0
npx create-pathfinder --mode orchestrator
```

## Every agent gets its own copy of the repository

This is the idea the whole thing rests on, and it is simpler than it sounds.

Git has a feature called a worktree. One repository can have several working directories checked out at the same time, each sitting on its own branch. Pathfinder gives every claimed ticket one of those, on a branch named after the ticket.

So two agents are never editing the same files in the same folder. They are not coordinating, or taking turns, or being careful. They are somewhere else entirely.

What stops two agents grabbing the same ticket is not a lock file I invented. It is a Git reference that Git creates only if it does not already exist. Of any number of agents trying to claim a ticket in the same moment, exactly one wins, and Git is what decides. No database, no background service, nothing running that you have to keep alive.

## How it decides who does what

Before dispatching anything, Pathfinder sizes up each ticket and writes down what it found:

```yaml
complexity:      medium      # 5 bullets under ## Changes
context:         small       # 3 paths named under ## Context
parallel-safety: isolated    # no other ticket is in flight
risk:            unassessed  # no risk rule applies
```

Each of those is a count over the ticket's own text rather than a guess. Complexity counts the bullet points describing the work. Context counts how many file paths the ticket names. The same ticket gives the same answer every time.

The interesting one is `parallel-safety`. It compares the files this ticket says it will touch against the files every other in-flight ticket says it will touch, and reports one of three things: `isolated` when there is no overlap, `shared-surface` when they share a directory, and `serialize` when they name the same file, which means do not run these two together.

That is the check that makes starting two agents feel less like a gamble.

Then a routing policy turns those findings into an assignment: which role the agent plays, which model it runs on, how much reasoning effort it gets.

## The boring version, on purpose

Here is the entire routing policy that ships:

```js
role:   session === "review" ? "tester" : "developer"
model:  "inherited"
effort: "inherited"
```

It reads none of the estimate. `inherited` means the agent simply runs on whatever the session that dispatched it was already using.

So Pathfinder measures four things and then ignores them. That is deliberate. Those measurements exist to be a stable input for a smarter policy later, and to sit visibly in every profile so I can find out whether they were any good in the first place. Nothing has tested those thresholds against real outcomes yet, and I would rather ship a policy whose behaviour I can predict than let an unvalidated number start choosing models for me.

Swapping in a different policy later means one new file and one line in the mode file. That socket was the actual deliverable.

## Then both agents died

The best evidence came from an accident.

During the acceptance run, Pathfinder dispatched two agents and correctly held a third ticket back, because it depended on the other two. Both agents then hit their usage limits and stopped, in the middle of their work.

Nothing was lost. The branch, the working directory, the uncommitted changes and the assignment all live in Git and the ticket tracker, not in the agent's memory. Fresh sessions picked up the same claims exactly where the dead ones left them, and the run finished.

An agent is the cheapest part of the system. It is the one piece you should be able to lose.

To be straight about the limits: those two agents never hit a human approval gate or a file conflict, so both of those were tested separately in throwaway repositories. And nothing in this release measures whether running agents in parallel is actually faster, so I am not claiming that it is.

## What it will not do

It does not merge anything. It does not skip review. It does not launch unlimited agents. A ticket whose status it cannot read clearly is never eligible to run, and never unblocks anything else.

Pathfinder can now run several agents at once. It still cannot approve, accept, merge or release anything. That is the whole point.

**Pathfinder on GitHub:** https://github.com/rikilamadrid/pathfinder

🧭 **The kit owns the workflow. The project owns the stack.**

<!-- locale:es -->

Pathfinder es el kit de flujo de trabajo que uso para construir software con agentes de IA. Hasta ahora hacía una cosa a la vez, a propósito. La versión 4.4.0 ya está disponible, y puede ejecutar varios agentes a la vez sin renunciar a lo que lo hace seguro.

Esto es lo que significa en la práctica.

## La parte lenta nunca fue pensar

El kit existe porque un agente escribe código mucho más rápido de lo que nadie puede aprobarlo con responsabilidad. Así que puse a un humano en cada punto donde una decisión tenía consecuencias: aprobar el plan, aceptar el trabajo, mergearlo, publicarlo.

Funcionó. También se volvió lento, por un motivo algo tonto.

Dos tickets que tocaban partes completamente distintas de un código seguían ejecutándose uno detrás de otro, porque había una sesión y yo estaba dentro. La cola no esperaba a que yo pensara. Esperaba a que yo estuviera disponible. Eso no es un humano en el bucle, es un humano haciendo de planificador.

## El modo nuevo

Un proyecto ahora dice cómo quiere ejecutarse, en una línea de un archivo:

```text
<!-- pathfinder:execution-mode orchestrator -->
```

`human-in-the-loop` es el comportamiento de siempre: un ticket, una sesión, tú conduces. `orchestrator` deja que Pathfinder trabaje en varios tickets a la vez, dé a cada uno su propio agente, y te interrumpa solo cuando algo necesita de verdad a una persona.

Un proyecto sin archivo de modo conserva el comportamiento anterior. No hay nada que migrar.

```bash
npx create-pathfinder@4.4.0
npx create-pathfinder --mode orchestrator
```

## Cada agente recibe su propia copia del repositorio

Esta es la idea sobre la que se apoya todo, y es más simple de lo que parece.

Git tiene una función llamada worktree. Un mismo repositorio puede tener varios directorios de trabajo abiertos a la vez, cada uno sobre su propia rama. Pathfinder le da uno a cada ticket reclamado, en una rama con el nombre del ticket.

Así que dos agentes nunca editan los mismos ficheros en la misma carpeta. No se coordinan, ni se turnan, ni tienen que ir con cuidado. Están en otro sitio.

Lo que impide que dos agentes cojan el mismo ticket no es un fichero de bloqueo inventado por mí. Es una referencia de Git que Git solo crea si no existe ya. De cualquier número de agentes intentando reclamar un ticket en el mismo instante, exactamente uno gana, y quien decide es Git. Sin base de datos, sin servicio en segundo plano, sin nada que haya que mantener vivo.

## Cómo decide quién hace qué

Antes de despachar nada, Pathfinder mide cada ticket y anota lo que encuentra:

```yaml
complexity:      medium      # 5 bullets under ## Changes
context:         small       # 3 paths named under ## Context
parallel-safety: isolated    # no other ticket is in flight
risk:            unassessed  # no risk rule applies
```

Cada uno de esos valores es un recuento sobre el propio texto del ticket, no una suposición. La complejidad cuenta los puntos que describen el trabajo. El contexto cuenta cuántas rutas de fichero nombra el ticket. El mismo ticket da la misma respuesta siempre.

El interesante es `parallel-safety`. Compara los ficheros que este ticket dice que va a tocar con los que dice tocar cada otro ticket en vuelo, y responde una de tres cosas: `isolated` si no hay solape, `shared-surface` si comparten un directorio, y `serialize` si nombran el mismo fichero, lo que significa no ejecutar estos dos juntos.

Esa es la comprobación que hace que lanzar dos agentes deje de parecer una apuesta.

Después, una política de enrutado convierte esos hallazgos en una asignación: qué rol juega el agente, sobre qué modelo corre, cuánto esfuerzo de razonamiento recibe.

## La versión aburrida, a propósito

Esta es la política de enrutado completa que se publica:

```js
role:   session === "review" ? "tester" : "developer"
model:  "inherited"
effort: "inherited"
```

No lee nada de la estimación. `inherited` significa que el agente simplemente corre sobre lo mismo que ya usaba la sesión que lo despachó.

Así que Pathfinder mide cuatro cosas y después las ignora. Es deliberado. Esas medidas existen para ser una entrada estable de una política más lista más adelante, y para quedar a la vista en cada perfil y que yo pueda averiguar si valían algo. Nada ha contrastado todavía esos umbrales con resultados reales, y prefiero publicar una política cuyo comportamiento puedo predecir antes que dejar que un número sin validar empiece a elegir modelos por mí.

Cambiar a otra política más adelante significa un fichero nuevo y una línea en el fichero de modo. Ese zócalo era el entregable real.

## Y entonces murieron los dos agentes

La mejor prueba vino de un accidente.

Durante la ejecución de aceptación, Pathfinder despachó dos agentes y retuvo correctamente un tercer ticket, porque dependía de los otros dos. Los dos agentes alcanzaron entonces sus límites de uso y se detuvieron, en mitad del trabajo.

No se perdió nada. La rama, el directorio de trabajo, los cambios sin commitear y la asignación viven en Git y en el tracker de tickets, no en la memoria del agente. Sesiones nuevas retomaron los mismos claims exactamente donde los dejaron los muertos, y la ejecución terminó.

Un agente es la parte más barata del sistema. Es la pieza que deberías poder perder.

Para ser claro con los límites: esos dos agentes nunca llegaron a un gate de aprobación humana ni a un conflicto de ficheros, así que ambas cosas se probaron aparte en repositorios desechables. Y nada en esta release mide si ejecutar agentes en paralelo es realmente más rápido, así que no afirmo que lo sea.

## Lo que no hará

No mergea nada. No se salta la revisión. No lanza agentes ilimitados. Un ticket cuyo estado no puede leer con claridad nunca es elegible para ejecutarse, y nunca desbloquea nada más.

Pathfinder ya puede ejecutar varios agentes a la vez. Sigue sin poder aprobar, aceptar, mergear ni publicar nada. Ese es justamente el objetivo.

**Pathfinder en GitHub:** https://github.com/rikilamadrid/pathfinder

🧭 **El kit es dueño del flujo de trabajo. El proyecto es dueño del stack.**

<!-- locale:fr -->

Pathfinder est le kit de workflow que j'utilise pour construire des logiciels avec des agents IA. Jusqu'ici, il faisait une chose à la fois, délibérément. La version 4.4.0 est disponible, et elle peut faire tourner plusieurs agents en parallèle sans renoncer à ce qui la rend sûre.

Voici ce que cela veut dire concrètement.

## La lenteur n'a jamais été la réflexion

Le kit existe parce qu'un agent écrit du code bien plus vite que quiconque peut l'approuver sérieusement. J'ai donc placé un humain à chaque endroit où une décision avait des conséquences : approuver le plan, accepter le travail, le merger, le publier.

Ça marchait. C'est aussi devenu lent, pour une raison un peu bête.

Deux tickets touchant des parties totalement indépendantes d'un code s'exécutaient quand même l'un après l'autre, parce qu'il y avait une seule session et que j'étais dedans. La file n'attendait pas que je réfléchisse. Elle attendait que je sois disponible. Ce n'est pas un humain dans la boucle, c'est un humain en guise d'ordonnanceur.

## Le nouveau mode

Un projet dit désormais comment il veut tourner, en une ligne dans un fichier :

```text
<!-- pathfinder:execution-mode orchestrator -->
```

`human-in-the-loop`, c'est l'ancien comportement : un ticket, une session, vous conduisez. `orchestrator` laisse Pathfinder travailler sur plusieurs tickets à la fois, donner à chacun son propre agent, et ne vous interrompre que lorsque quelque chose a vraiment besoin d'une personne.

Un projet sans fichier de mode conserve l'ancien comportement. Il n'y a rien à migrer.

```bash
npx create-pathfinder@4.4.0
npx create-pathfinder --mode orchestrator
```

## Chaque agent reçoit sa propre copie du dépôt

C'est l'idée sur laquelle tout repose, et elle est plus simple qu'il n'y paraît.

Git possède une fonctionnalité appelée worktree. Un même dépôt peut avoir plusieurs répertoires de travail ouverts en même temps, chacun sur sa propre branche. Pathfinder en donne un à chaque ticket réclamé, sur une branche portant le nom du ticket.

Deux agents ne modifient donc jamais les mêmes fichiers dans le même dossier. Ils ne se coordonnent pas, ne prennent pas leur tour, n'ont pas à faire attention. Ils sont ailleurs.

Ce qui empêche deux agents de prendre le même ticket n'est pas un fichier de verrou que j'aurais inventé. C'est une référence Git que Git ne crée que si elle n'existe pas déjà. Sur un nombre quelconque d'agents tentant de réclamer un ticket au même instant, exactement un l'emporte, et c'est Git qui tranche. Pas de base de données, pas de service en arrière-plan, rien à maintenir en vie.

## Comment il décide qui fait quoi

Avant d'envoyer quoi que ce soit, Pathfinder évalue chaque ticket et note ce qu'il trouve :

```yaml
complexity:      medium      # 5 bullets under ## Changes
context:         small       # 3 paths named under ## Context
parallel-safety: isolated    # no other ticket is in flight
risk:            unassessed  # no risk rule applies
```

Chacune de ces valeurs est un décompte sur le texte même du ticket, pas une supposition. La complexité compte les puces qui décrivent le travail. Le contexte compte le nombre de chemins de fichiers que le ticket nomme. Le même ticket donne toujours la même réponse.

La plus intéressante est `parallel-safety`. Elle compare les fichiers que ce ticket annonce toucher à ceux qu'annoncent tous les autres tickets en vol, et répond l'une de trois choses : `isolated` s'il n'y a aucun recouvrement, `shared-surface` s'ils partagent un répertoire, et `serialize` s'ils nomment le même fichier, ce qui signifie ne pas lancer ces deux-là ensemble.

C'est ce contrôle qui fait que lancer deux agents cesse de ressembler à un pari.

Ensuite, une politique de routage transforme ces constats en affectation : quel rôle joue l'agent, sur quel modèle il tourne, quel effort de raisonnement il reçoit.

## La version ennuyeuse, à dessein

Voici l'intégralité de la politique de routage livrée :

```js
role:   session === "review" ? "tester" : "developer"
model:  "inherited"
effort: "inherited"
```

Elle ne lit rien de l'estimation. `inherited` signifie que l'agent tourne simplement sur ce qu'utilisait déjà la session qui l'a envoyé.

Pathfinder mesure donc quatre choses, puis les ignore. C'est délibéré. Ces mesures existent pour servir d'entrée stable à une politique plus fine plus tard, et pour rester visibles dans chaque profil afin que je puisse découvrir si elles valaient quelque chose. Rien n'a encore confronté ces seuils à des résultats réels, et je préfère livrer une politique dont je peux prédire le comportement plutôt que laisser un nombre non validé se mettre à choisir des modèles à ma place.

Passer à une autre politique plus tard, c'est un nouveau fichier et une ligne dans le fichier de mode. Cet emplacement était le vrai livrable.

## Et puis les deux agents sont morts

La meilleure preuve est venue d'un accident.

Pendant le run d'acceptation, Pathfinder a envoyé deux agents et retenu correctement un troisième ticket, parce qu'il dépendait des deux autres. Les deux agents ont alors atteint leurs limites d'usage et se sont arrêtés, en plein travail.

Rien n'a été perdu. La branche, le répertoire de travail, les modifications non commitées et l'affectation vivent dans Git et dans le tracker de tickets, pas dans la mémoire de l'agent. De nouvelles sessions ont repris les mêmes claims exactement là où les agents morts les avaient laissés, et le run est allé à son terme.

Un agent est la pièce la moins chère du système. C'est celle qu'on devrait pouvoir perdre.

Pour être net sur les limites : ces deux agents n'ont jamais atteint de gate d'approbation humaine ni de conflit de fichiers, donc ces deux cas ont été testés séparément dans des dépôts jetables. Et rien dans cette version ne mesure si faire tourner des agents en parallèle est réellement plus rapide, donc je ne prétends pas que ça l'est.

## Ce qu'il ne fera pas

Il ne merge rien. Il ne saute pas la revue. Il ne lance pas d'agents illimités. Un ticket dont il ne sait pas lire clairement l'état n'est jamais éligible, et ne débloque jamais rien d'autre.

Pathfinder peut désormais faire tourner plusieurs agents en parallèle. Il ne peut toujours pas approuver, accepter, merger ni publier quoi que ce soit. C'est tout l'intérêt.

**Pathfinder sur GitHub :** https://github.com/rikilamadrid/pathfinder

🧭 **Le kit possède le workflow. Le projet possède la stack.**
