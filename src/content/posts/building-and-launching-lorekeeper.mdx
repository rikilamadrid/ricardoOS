---
title:
  en: "Shipping Lorekeeper: from a note I couldn't find to npm"
  es: "Publicar Lorekeeper: de una nota que no encontraba a npm"
  fr: "Livrer Lorekeeper : d'une note introuvable à npm"
summary:
  en: How I took Lorekeeper, an offline CLI that finds the passage in your Markdown notes that answers a question, from idea to an npm package, a docs site, an identity and a release pipeline.
  es: Cómo llevé Lorekeeper, una CLI offline que encuentra en tus notas Markdown el pasaje que responde a una pregunta, desde la idea hasta un paquete npm, un sitio de documentación, una identidad y un pipeline de releases.
  fr: Comment j'ai mené Lorekeeper, une CLI hors ligne qui trouve dans vos notes Markdown le passage qui répond à une question, de l'idée à un package npm, un site de documentation, une identité et un pipeline de release.
shareCaption:
  en: "You already wrote it down. Find the passage that answers. 📜 Lorekeeper is on npm: offline, deterministic, no API key. Here's what it took to ship it, including the release run that failed."
  es: "Ya lo escribiste. Encuentra el pasaje que responde. 📜 Lorekeeper está en npm: offline, determinista, sin API key. Esto es lo que hizo falta para publicarlo, incluida la release que falló."
  fr: "Vous l'avez déjà écrit. Trouvez le passage qui répond. 📜 Lorekeeper est sur npm : hors ligne, déterministe, sans clé d'API. Voici ce qu'il a fallu pour le livrer, y compris la release qui a échoué."
date: "2026-09-25"
tags: ["ai", "engineering", "lorekeeper", "second-brain", "open-source", "typescript"]
draft: false
---

<!-- locale:en -->

I have said "I already wrote this down somewhere" more times than I'd like to admit.

It's usually true. The note exists, in a Markdown file I wrote months ago, under a heading I don't remember. The knowledge is there. It just isn't *recoverable*, and knowledge you can't recover is about as useful as knowledge you never had.

Lorekeeper started as my answer to that sentence. This week it became a real thing: an npm package, a documentation site, its own identity and a release pipeline. Here are the four decisions I'd make again.

**You already wrote it down. Find the passage that answers.**

That's the tagline, and the important word is *passage*.

Lorekeeper is a CLI with three commands. `lore init` creates a brain, which is just a directory of Markdown, or adopts a vault you already have without rewriting a file. `lore capture` files a thought or a URL. `lore search` ranks the passages of that brain against a question.

```sh
npx create-lorekeeper@0.1.0 init ~/brain
```

It runs offline. No account, no API key, no network call, no language model. Node 24+, macOS or Linux.

## 1. The passage is the unit, not the file

A three-thousand-word note about deployments isn't one fact. It's forty. Return the whole file and you get the answer plus thirty-nine things competing with it, which is bad for me and worse for an agent paying for every byte of context. (The longer version is in [Lorekeeper retrieves spans](/writing/lorekeeper-retrieves-spans).)

So every result is an address: path, heading, line range, score and text. A real run, asking one question two ways:

```sh
$ npx create-lorekeeper@0.1.0 search ~/brain \
    "retry policy" "what happens when the upload fails" --limit 2
notes/deploy-retries.md#When the upload fails:12-13  score 0.0164
~/brain/notes/deploy-retries.md
    The FTP step retries three times with a thirty-second backoff, then gives up.
    If it gives up, re-run the workflow; it only uploads changed files.

notes/README.md#notes:3-8  score 0.0161
~/brain/notes/README.md
    What you have worked out, written in your own words.
    ...
```

Two lines, not the file. The note never says "retry policy". The second wording found it.

## No fake certainty

Look at the second hit. It's a README with nothing to do with retries, scoring 0.0161 against the right answer's 0.0164.

Those numbers aren't confidence. Lorekeeper fuses the ranked lists with Reciprocal Rank Fusion, and 0.0164 is just 1/61, the score for ranking first. **A ranking score cannot prove that something is absent.** In evaluation, a control question with no answer anywhere outranked real questions that had one.

So there's no relevance threshold. A cutoff would turn an honest miss into a confident "nothing found". Instead, the `AGENTS.md` that `lore init` writes tells agents to treat results as evidence, search again in other words, and say "the evidence is insufficient" rather than "your notes don't cover this". I'd rather ship a tool exactly as smart as it is.

## Giving it a face

Lorekeeper is one of four tools in my Wonder Wagon family, with Pathfinder, Forge and Lama. Its role is the reading room, the quietest of the four. The mark is a four-point star held in a ring: what was found, and what keeps it.

![The Lorekeeper documentation site in day mode.](/writing/building-and-launching-lorekeeper/home-day.png)

Day and night each get a measured palette. At a colour terminal, `lore --help` paints `Lorekeeper` in periwinkle `#99A2F0` and ends with a dimmed serial, `LK-047`, next to Pathfinder's `PF-047` and Forge's `FG-047`. Piped output and `--json` carry no escape codes.

![The same page at night.](/writing/building-and-launching-lorekeeper/home-night.png)

## 2. Brand consistency as a failing test

The colours live in two repositories. Lorekeeper's `tokens.json` is the source, and the Wonder Wagon UI library restates nine of those values in its `lorekeeper` theme. When I promoted that theme from `draft` to `pilot`, the night accent changed.

Copied values drift, so both sides now check the same nine-value map. Run against the old Wonder Wagon pin, Lorekeeper's check fails on `accent.night` and `accentInk.night`, which is exactly right. Add measured contrast (52 pairs) and byte-for-byte checks on the README images, and "on brand" stops being a review opinion. It's a CI step.

## 3. One public package, private core

`@lorekeeper/core` does the real work and is private. The CLI imports it by name, and that import can't survive publishing. npm would look for a package that doesn't exist, and the install would die with a 404.

So the build inlines core into one file, `dist/lore.js`, with esbuild, then checks its own output: only `node:*` and `yaml` imports allowed, and core must actually be inside. A package check on every PR installs the tarball into a fresh prefix and runs the real `lore`. The build is deterministic, so the sha1 I approved, `3ef401e3…`, is the one npm serves today.

## 4. Release evidence describes what happened

Before the release, the README said Lorekeeper installed from source. True, until release day. I didn't "fix" it early. It changed in the release PR itself, and the docs site waited until the package was actually on npm.

The site enforces this mechanically. Every command output on it is generated from the built binary, and a Vercel build refuses to deploy if that binary reports `0.0.0`.

![The live CLI reference page showing lore --help reading Lorekeeper 0.1.0.](/writing/building-and-launching-lorekeeper/cli-night.png)

The version comes from one place, the newest `CHANGELOG.md` heading, and the release workflow refuses if the manifest, tag or Release disagree. Every step skips work that's already done, so a failed run is re-run, never patched with a new version.

That got tested immediately. The first release run failed because `npm@latest` installed npm 12, which prints `npm pack --json` as an object instead of an array. Checks run before publishing, so nothing half-shipped. I taught the script both shapes, re-ran it, and it tagged `v0.1.0`.

## What I learned

Ranking the right passage was the part I expected to be hard. Most of the work after that was making every public claim true: the README colour, the help-screen version, the install line, the tag.

What worked was giving each claim a check a machine runs. None of it is glamorous, but when the release broke on a JSON shape I never touched, nothing shipped broken. It stopped and told me why.

The docs are at [lorekeeper-bay.vercel.app](https://lorekeeper-bay.vercel.app) and work offline after one visit. The code is on [GitHub](https://github.com/rikilamadrid/lorekeeper). If Lorekeeper finds something you forgot you wrote, I'd like to hear about it.

<!-- locale:es -->

He dicho "esto ya lo apunté en algún sitio" más veces de las que me gustaría admitir.

Suele ser verdad. La nota existe, en un fichero Markdown que escribí hace meses, bajo un encabezado que no recuerdo. El conocimiento está ahí. Simplemente no se puede *recuperar*, y un conocimiento que no puedes recuperar vale más o menos lo mismo que uno que nunca tuviste.

Lorekeeper nació como mi respuesta a esa frase. Esta semana se convirtió en algo real: un paquete npm, un sitio de documentación, una identidad propia y un pipeline de releases. Estas son las cuatro decisiones que volvería a tomar.

**You already wrote it down. Find the passage that answers.** Ya lo escribiste. Encuentra el pasaje que responde.

Ese es el eslogan, y la palabra importante es *pasaje*.

Lorekeeper es una CLI con tres comandos. `lore init` crea un cerebro, que no es más que un directorio de Markdown, o adopta una bóveda que ya tienes sin reescribir ningún fichero. `lore capture` archiva una idea o una URL. `lore search` ordena los pasajes de ese cerebro frente a una pregunta.

```sh
npx create-lorekeeper@0.1.0 init ~/brain
```

Funciona sin conexión. Sin cuenta, sin API key, sin llamadas de red, sin modelo de lenguaje. Node 24+, macOS o Linux.

## 1. La unidad es el pasaje, no el fichero

Una nota de tres mil palabras sobre despliegues no es un dato. Son cuarenta. Si devuelves el fichero entero, recibes la respuesta más treinta y nueve cosas compitiendo con ella, lo cual es malo para mí y peor para un agente que paga cada byte de contexto. (La versión larga está en [Lorekeeper recupera fragmentos](/writing/lorekeeper-retrieves-spans).)

Así que cada resultado es una dirección: ruta, encabezado, rango de líneas, puntuación y texto. Una ejecución real, con la misma pregunta formulada de dos maneras:

```sh
$ npx create-lorekeeper@0.1.0 search ~/brain \
    "retry policy" "what happens when the upload fails" --limit 2
notes/deploy-retries.md#When the upload fails:12-13  score 0.0164
~/brain/notes/deploy-retries.md
    The FTP step retries three times with a thirty-second backoff, then gives up.
    If it gives up, re-run the workflow; it only uploads changed files.

notes/README.md#notes:3-8  score 0.0161
~/brain/notes/README.md
    What you have worked out, written in your own words.
    ...
```

Dos líneas, no el fichero. La nota nunca dice "retry policy". La segunda formulación la encontró.

## Sin certezas falsas

Mira el segundo resultado. Es un README que no tiene nada que ver con reintentos, con 0,0161 frente al 0,0164 de la respuesta correcta.

Esos números no son confianza. Lorekeeper fusiona las listas con Reciprocal Rank Fusion, y 0,0164 es simplemente 1/61, la puntuación de quedar primero. **Una puntuación de ranking no puede demostrar que algo no está.** En la evaluación, una pregunta de control sin respuesta en ningún sitio quedó por encima de preguntas reales que sí la tenían.

Por eso no hay umbral de relevancia. Un corte convertiría un fallo honesto en un "no se encontró nada" muy seguro de sí mismo. En su lugar, el `AGENTS.md` que escribe `lore init` le dice a los agentes que traten los resultados como evidencia, que vuelvan a buscar con otras palabras y que digan "la evidencia es insuficiente" en vez de "tus notas no cubren esto". Prefiero publicar una herramienta exactamente tan lista como es.

## Darle una cara

Lorekeeper es una de las cuatro herramientas de mi familia Wonder Wagon, junto a Pathfinder, Forge y Lama. Su papel es la sala de lectura, la más silenciosa de las cuatro. La marca es una estrella de cuatro puntas dentro de un anillo: lo que se encontró y lo que lo guarda.

![El sitio de documentación de Lorekeeper en modo día.](/writing/building-and-launching-lorekeeper/home-day.png)

Día y noche tienen cada uno una paleta medida. En un terminal con color, `lore --help` pinta `Lorekeeper` en pervinca `#99A2F0` y termina con un número de serie atenuado, `LK-047`, junto al `PF-047` de Pathfinder y el `FG-047` de Forge. La salida por tubería y `--json` no llevan códigos de escape.

![La misma página de noche.](/writing/building-and-launching-lorekeeper/home-night.png)

## 2. La coherencia de marca como un test que falla

Los colores viven en dos repositorios. El `tokens.json` de Lorekeeper es la fuente, y la librería de UI de Wonder Wagon repite nueve de esos valores en su tema `lorekeeper`. Cuando promoví ese tema de `draft` a `pilot`, el acento nocturno cambió.

Los valores copiados se desvían, así que ahora los dos lados comprueban el mismo mapa de nueve valores. Ejecutado contra el pin antiguo de Wonder Wagon, el check de Lorekeeper falla en `accent.night` y `accentInk.night`, que es exactamente lo correcto. Súmale el contraste medido (52 pares) y las comprobaciones byte a byte de las imágenes del README, y "estar en marca" deja de ser una opinión de revisión. Es un paso de CI.

## 3. Un paquete público, un núcleo privado

`@lorekeeper/core` hace el trabajo de verdad y es privado. La CLI lo importa por nombre, y ese import no sobrevive a la publicación. npm buscaría un paquete que no existe y la instalación moriría con un 404.

Así que la build integra el núcleo en un único fichero, `dist/lore.js`, con esbuild, y luego comprueba su propia salida: solo se permiten imports `node:*` y `yaml`, y el núcleo tiene que estar realmente dentro. Una verificación del paquete en cada PR instala el tarball en un prefijo limpio y ejecuta el `lore` real. La build es determinista, así que el sha1 que aprobé, `3ef401e3…`, es el que sirve npm hoy.

## 4. La evidencia de una release describe lo que pasó

Antes de la release, el README decía que Lorekeeper se instalaba desde el código fuente. Era verdad, hasta el día de la release. No lo "arreglé" antes. Cambió en el propio PR de la release, y el sitio de documentación esperó a que el paquete estuviera de verdad en npm.

El sitio lo aplica de forma mecánica. Cada salida de comando que muestra se genera desde el binario construido, y una build de Vercel se niega a desplegar si ese binario dice `0.0.0`.

![La página de referencia de la CLI en producción, con lore --help mostrando Lorekeeper 0.1.0.](/writing/building-and-launching-lorekeeper/cli-night.png)

La versión sale de un único sitio, el encabezado más reciente de `CHANGELOG.md`, y el workflow de release se niega a seguir si el manifiesto, el tag o la Release no coinciden. Cada paso se salta el trabajo que ya está hecho, así que una ejecución fallida se relanza, nunca se parchea con una versión nueva.

Eso se puso a prueba enseguida. La primera ejecución de la release falló porque `npm@latest` instaló npm 12, que imprime `npm pack --json` como un objeto en vez de un array. Las comprobaciones van antes de publicar, así que nada quedó a medias. Le enseñé al script las dos formas, la relancé y etiquetó `v0.1.0`.

## Lo que aprendí

Ordenar bien el pasaje correcto era la parte que esperaba que fuera difícil. La mayor parte del trabajo posterior fue hacer verdad cada afirmación pública: el color del README, la versión de la ayuda, la línea de instalación, el tag.

Lo que funcionó fue darle a cada afirmación una comprobación que ejecuta una máquina. Nada de eso es glamuroso, pero cuando la release se rompió por la forma de un JSON que yo nunca toqué, no se publicó nada roto. Se paró y me dijo por qué.

La documentación está en [lorekeeper-bay.vercel.app](https://lorekeeper-bay.vercel.app) y funciona sin conexión tras una visita. El código está en [GitHub](https://github.com/rikilamadrid/lorekeeper). Si Lorekeeper encuentra algo que habías olvidado que escribiste, me gustaría saberlo.

<!-- locale:fr -->

J'ai dit "je l'ai déjà noté quelque part" plus souvent que je ne voudrais l'admettre.

C'est généralement vrai. La note existe, dans un fichier Markdown écrit il y a des mois, sous un titre dont je ne me souviens pas. Le savoir est là. Il n'est simplement pas *récupérable*, et un savoir qu'on ne peut pas récupérer vaut à peu près autant qu'un savoir qu'on n'a jamais eu.

Lorekeeper est né comme ma réponse à cette phrase. Cette semaine, c'est devenu quelque chose de réel : un package npm, un site de documentation, une identité propre et un pipeline de release. Voici les quatre décisions que je reprendrais.

**You already wrote it down. Find the passage that answers.** Vous l'avez déjà écrit. Trouvez le passage qui répond.

C'est le slogan, et le mot important, c'est *passage*.

Lorekeeper est une CLI à trois commandes. `lore init` crée un cerveau, qui n'est qu'un répertoire de Markdown, ou adopte un coffre existant sans réécrire un seul fichier. `lore capture` range une idée ou une URL. `lore search` classe les passages de ce cerveau par rapport à une question.

```sh
npx create-lorekeeper@0.1.0 init ~/brain
```

Il fonctionne hors ligne. Pas de compte, pas de clé d'API, pas d'appel réseau, pas de modèle de langage. Node 24+, macOS ou Linux.

## 1. L'unité, c'est le passage, pas le fichier

Une note de trois mille mots sur les déploiements n'est pas un fait. C'en est quarante. Renvoyez le fichier entier et vous obtenez la réponse plus trente-neuf choses qui lui font concurrence, ce qui est gênant pour moi et pire pour un agent qui paie chaque octet de contexte. (La version longue est dans [Lorekeeper récupère des fragments](/writing/lorekeeper-retrieves-spans).)

Chaque résultat est donc une adresse : chemin, titre, plage de lignes, score et texte. Une exécution réelle, avec la même question posée de deux façons :

```sh
$ npx create-lorekeeper@0.1.0 search ~/brain \
    "retry policy" "what happens when the upload fails" --limit 2
notes/deploy-retries.md#When the upload fails:12-13  score 0.0164
~/brain/notes/deploy-retries.md
    The FTP step retries three times with a thirty-second backoff, then gives up.
    If it gives up, re-run the workflow; it only uploads changed files.

notes/README.md#notes:3-8  score 0.0161
~/brain/notes/README.md
    What you have worked out, written in your own words.
    ...
```

Deux lignes, pas le fichier. La note ne dit jamais "retry policy". C'est la deuxième formulation qui l'a trouvée.

## Pas de fausse certitude

Regardez le deuxième résultat. C'est un README sans aucun rapport avec les nouvelles tentatives, à 0,0161 contre 0,0164 pour la bonne réponse.

Ces chiffres ne sont pas une confiance. Lorekeeper fusionne les listes par Reciprocal Rank Fusion, et 0,0164 vaut simplement 1/61, le score d'une première place. **Un score de classement ne peut pas prouver qu'une chose est absente.** Pendant l'évaluation, une question témoin sans réponse nulle part s'est classée au-dessus de vraies questions qui en avaient une.

Il n'y a donc aucun seuil de pertinence. Un seuil transformerait un échec honnête en un "rien trouvé" plein d'assurance. À la place, le `AGENTS.md` écrit par `lore init` dit aux agents de traiter les résultats comme des indices, de chercher à nouveau avec d'autres mots et de dire "les éléments sont insuffisants" plutôt que "vos notes n'en parlent pas". Je préfère livrer un outil exactement aussi malin qu'il l'est.

## Lui donner un visage

Lorekeeper est l'un des quatre outils de ma famille Wonder Wagon, avec Pathfinder, Forge et Lama. Son rôle, c'est la salle de lecture, la plus calme des quatre. La marque est une étoile à quatre branches tenue dans un anneau : ce qui a été trouvé, et ce qui le garde.

![Le site de documentation de Lorekeeper en mode jour.](/writing/building-and-launching-lorekeeper/home-day.png)

Le jour et la nuit ont chacun une palette mesurée. Dans un terminal couleur, `lore --help` peint `Lorekeeper` en pervenche `#99A2F0` et se termine par un numéro de série atténué, `LK-047`, à côté du `PF-047` de Pathfinder et du `FG-047` de Forge. La sortie redirigée et `--json` ne contiennent aucun code d'échappement.

![La même page, la nuit.](/writing/building-and-launching-lorekeeper/home-night.png)

## 2. La cohérence de marque comme un test qui échoue

Les couleurs vivent dans deux dépôts. Le `tokens.json` de Lorekeeper est la source, et la bibliothèque d'UI de Wonder Wagon reprend neuf de ces valeurs dans son thème `lorekeeper`. Quand j'ai promu ce thème de `draft` à `pilot`, l'accent de nuit a changé.

Les valeurs copiées dérivent, donc les deux côtés vérifient désormais la même table de neuf valeurs. Lancée contre l'ancien pin de Wonder Wagon, la vérification de Lorekeeper échoue sur `accent.night` et `accentInk.night`, ce qui est exactement le bon comportement. Ajoutez le contraste mesuré (52 paires) et les vérifications octet par octet des images du README, et "respecter la marque" cesse d'être une opinion de revue. C'est une étape de CI.

## 3. Un package public, un cœur privé

`@lorekeeper/core` fait le vrai travail et reste privé. La CLI l'importe par son nom, et cet import ne survit pas à la publication. npm chercherait un package qui n'existe pas, et l'installation mourrait sur un 404.

Le build intègre donc le cœur dans un seul fichier, `dist/lore.js`, avec esbuild, puis vérifie sa propre sortie : seuls les imports `node:*` et `yaml` sont autorisés, et le cœur doit réellement s'y trouver. Une vérification du package sur chaque PR installe le tarball dans un préfixe vierge et lance le vrai `lore`. Le build est déterministe, donc le sha1 que j'ai validé, `3ef401e3…`, est celui que npm sert aujourd'hui.

## 4. Les preuves d'une release décrivent ce qui s'est passé

Avant la release, le README disait que Lorekeeper s'installait depuis les sources. C'était vrai, jusqu'au jour de la release. Je ne l'ai pas "corrigé" en avance. Il a changé dans la PR de release elle-même, et le site de documentation a attendu que le package soit réellement sur npm.

Le site l'applique de façon mécanique. Chaque sortie de commande qu'il affiche est générée depuis le binaire construit, et un build Vercel refuse de déployer si ce binaire annonce `0.0.0`.

![La page de référence de la CLI en production, avec lore --help qui affiche Lorekeeper 0.1.0.](/writing/building-and-launching-lorekeeper/cli-night.png)

La version vient d'un seul endroit, le titre le plus récent de `CHANGELOG.md`, et le workflow de release refuse de continuer si le manifeste, le tag ou la Release ne concordent pas. Chaque étape saute le travail déjà fait, donc une exécution ratée est relancée, jamais rafistolée avec une nouvelle version.

Ça a été mis à l'épreuve tout de suite. La première exécution de la release a échoué parce que `npm@latest` a installé npm 12, qui affiche `npm pack --json` sous forme d'objet au lieu d'un tableau. Les vérifications passent avant la publication, donc rien n'a été livré à moitié. J'ai appris les deux formes au script, relancé, et il a tagué `v0.1.0`.

## Ce que j'ai appris

Bien classer le bon passage, c'était la partie que je pensais difficile. L'essentiel du travail ensuite a consisté à rendre vraie chaque affirmation publique : la couleur du README, la version de l'aide, la ligne d'installation, le tag.

Ce qui a marché, c'est de donner à chaque affirmation une vérification qu'une machine exécute. Rien de glamour, mais quand la release a cassé à cause de la forme d'un JSON auquel je n'avais jamais touché, rien de cassé n'a été livré. Elle s'est arrêtée et m'a dit pourquoi.

La documentation est sur [lorekeeper-bay.vercel.app](https://lorekeeper-bay.vercel.app) et fonctionne hors ligne après une visite. Le code est sur [GitHub](https://github.com/rikilamadrid/lorekeeper). Si Lorekeeper retrouve quelque chose que vous aviez oublié avoir écrit, j'aimerais le savoir.
