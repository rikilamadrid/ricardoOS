---
title:
  en: Pathfinder learned to run more than one agent at a time
  es: Pathfinder aprendió a ejecutar más de un agente a la vez
  fr: Pathfinder a appris à faire travailler plusieurs agents à la fois
summary:
  en: My workflow kit was built to keep a human in the loop, one ticket at a time. Now it can coordinate several isolated agents at once, without becoming an autopilot.
  es: Mi kit de flujo de trabajo estaba hecho para mantener a un humano en el bucle, ticket a ticket. Ahora puede coordinar varios agentes aislados a la vez, sin convertirse en un piloto automático.
  fr: Mon kit de workflow était conçu pour garder un humain dans la boucle, un ticket à la fois. Il peut désormais coordonner plusieurs agents isolés en parallèle, sans devenir un pilote automatique.
shareCaption:
  en: Pathfinder now has two execution modes. 🧭 One agent under direct control, or several isolated workers coordinated at once. The merge button is still mine.
  es: Pathfinder ya tiene dos modos de ejecución. 🧭 Un agente bajo control directo, o varios workers aislados coordinados a la vez. El botón de merge sigue siendo mío.
  fr: Pathfinder a maintenant deux modes d'exécution. 🧭 Un agent sous contrôle direct, ou plusieurs workers isolés coordonnés en parallèle. Le bouton merge reste le mien.
date: "2026-09-18"
tags: ["ai", "engineering", "pathfinder", "agents", "open-source"]
draft: false
---

<!-- locale:en -->

For months, Pathfinder did one thing very deliberately: it made me the bottleneck.

Load a ticket. Start it. Ask for a review. Accept the result. Merge it. One at a time.

That was never an accident. The kit exists because a coding agent can execute far faster than it can be trusted, and the gap between those two speeds is exactly where bad software gets written.

So I put a human in every place where a decision had consequences.

It worked. It also got slow for a reason that had nothing to do with judgment.

## The waiting that wasn't mine

Most of the queue wasn't waiting on me thinking.

It was waiting on me *being available*.

Two tickets that touch completely different parts of a codebase, with no dependency between them, would still run one after the other, because there was exactly one session and I was in it.

That's not human-in-the-loop. That's human-as-a-scheduler.

So Pathfinder now has two execution modes over the same ticket lifecycle.

## One marker line

The mode lives in a tracked file, `context/execution-mode.md`, and the part the tool actually reads is one line:

```text
<!-- pathfinder:execution-mode orchestrator -->
```

There are two valid values, `human-in-the-loop` and `orchestrator`. A missing file means human-in-the-loop, so every project installed before modes existed needs no migration at all.

A file with an invalid marker doesn't get guessed at. The ticket lifecycle says so once and proceeds human-in-the-loop. Orchestration refuses to run.

You choose it at install, or switch later:

```bash
npx create-pathfinder --mode orchestrator
```

## What a worker actually gets

A claim is not a label. It's a Git fact.

Claiming a ticket creates a linked worktree at `.pathfinder/worktrees/<key>` and its own `ticket/<key>-<slug>` branch. What stops two workers owning the same ticket is Git's own claim ref and worktree checks, not a lock file I invented.

Each worktree carries its own `context/current-ticket.md`, so one worker's state can never overwrite another's. That single detail is the reason parallel work stops being terrifying.

## The plan you approve, once

`start` plans before it claims.

A ticket is eligible only when its status is `Proposed` or `Ready` and every blocker is `Complete`.

Before anything is claimed, you see each selected ticket, its estimate, role, model, effort, and proposed worker assignment. Then you approve once.

That approval covers execution, claims, ticket-branch commits and pushes, draft PRs, and tracker writes.

It does **not** cover merge.

The worker limit defaults to three.

## Estimates are evidence, not promises

Every dispatch exposes the same shape:

```text
estimate(ticket, in-flight claims) → execution profile
select(profile, policy)            → role, model, effort
dispatch(brief)                    → worker session
```

The profile records complexity, context size, parallel safety and risk, with the reason for each and whether it was derived or assessed. Parallel safety reports `isolated`, `shared-surface` or `serialize`.

I'm careful about how much weight that carries. Those estimates come from the paths a ticket declares, so they are coarse evidence that two branches probably won't collide, not proof. Actual changed-file overlap gets checked again at integration.

This release ships **static** routing: `developer` implements, `tester` reviews, and the model and reasoning effort are inherited from the dispatching session. Dynamic model routing is not in it. I'd rather ship the boring version of a routing layer and learn what it needs.

Harness translation covers `claude-code`, `codex` and `manual`. A value the harness can't honor is refused out loud rather than quietly downgraded.

## Gates are the whole point

When a worker hits something that needs a person, it records `State: human-gate` and the exact question.

Its dependents wait. Everything unrelated keeps going.

On GitHub Issues the orchestrator adds a `gate: human` label and one idempotent comment, so a retry can't spam the issue.

Execution state (`ready`, `working`, `review`, `human-gate`, `done`, `stale`, `failed`, `integrated`) is a different vocabulary from ticket lifecycle (`Proposed → Ready → In Progress → Complete`). Conflating them is how a tool starts quietly deciding things.

## Integration is a separate decision

Work complete is not the same as safe to merge.

The integrator takes reviewed, pushed work and checks it against the current default branch: changed-file overlap, divergence, `git merge-tree` conflict evidence, review, CI. It returns one of three answers.

`candidate`: present the merge under your approval policy.

`behind`: resume the existing developer to update onto the default branch and re-verify.

`conflict`: serialize the conflicting work and hand it back to its developer.

Because a merge changes the evidence, every remaining branch gets checked again afterward. A behind branch is never dressed up as a clean candidate.

## It survived its own acceptance run

The part I actually care about isn't the design. It's that the feature was built by the feature, and the run went wrong in a useful way.

Pathfinder selected two tickets to run concurrently while a third stayed correctly blocked. It created distinct claims, branches and worktrees, and dispatched Codex workers.

Then both worker sessions hit usage limits and ended.

The work survived. The branch, worktree, claim, state file and execution profile are all durable, so replacement sessions resumed the same claims and carried on.

One ticket integrated first. The orchestrator reported the second as `behind`, dispatched a rebase-and-reverify brief against its preserved worktree, and only after fresh review and integration did the real dependency graph make the third ticket eligible.

I didn't plan that failure. It's the best evidence in the whole Feature.

A quota limit, a crash or a terminated harness does not erase a ticket:

```text
/orchestrate resume 53.4
```

A compatible harness can even replace the worker without replacing the ticket. A Claude Code session on an inherited profile can be resumed by Codex in the same worktree and branch. There's no automatic failover, and I don't want one. Two sessions mutating one worktree is the failure mode worth being paranoid about.

## What it is not

No daemon. No broker. No queue. No database. No server.

No automatic merging, no bypassing review, no unlimited agents.

The engine reads the ticket store, the blockers, Git branches and each worker's state file every time it runs. There is nothing to start and nothing to keep alive. It needs Node and Git, plus `gh` if your tickets live in GitHub Issues, while the human-in-the-loop workflow still needs no runtime at all.

## Where it sits right now

Nine tickets, nine squash-merged pull requests, and one acceptance run with its snapshots and transcripts recorded rather than summarized. Every suite green: orchestration 166/166, installer 693/693, renderer 825 passing with 25 documented skips.

No version bump, no npm publication, no tag, no release. The capability sits under `[Unreleased]` until I decide it's worth shipping, which is its own decision and not the same as finishing the work.

Pathfinder can now run several agents at once. It still can't approve, accept, merge or release anything.

**Pathfinder on GitHub:** https://github.com/rikilamadrid/pathfinder

🧭 **The kit owns the workflow. The project owns the stack.**

<!-- locale:es -->

Durante meses, Pathfinder hizo una cosa de forma muy deliberada: convertirme a mí en el cuello de botella.

Cargar un ticket. Empezarlo. Pedir una revisión. Aceptar el resultado. Mergearlo. De uno en uno.

Eso nunca fue un accidente. El kit existe porque un agente de código puede ejecutar mucho más rápido de lo que se le puede confiar, y la distancia entre esas dos velocidades es exactamente donde se escribe el software malo.

Así que puse a un humano en cada punto donde una decisión tenía consecuencias.

Funcionó. También se volvió lento por un motivo que no tenía nada que ver con el criterio.

## La espera que no era mía

La mayor parte de la cola no esperaba a que yo pensara.

Esperaba a que yo *estuviera disponible*.

Dos tickets que tocan partes completamente distintas de un código, sin ninguna dependencia entre ellos, se ejecutaban uno detrás de otro, porque había exactamente una sesión y yo estaba dentro.

Eso no es *human-in-the-loop*. Eso es un humano haciendo de planificador.

Así que Pathfinder ahora tiene dos modos de ejecución sobre el mismo ciclo de vida de tickets.

## Una línea marcadora

El modo vive en un archivo versionado, `context/execution-mode.md`, y lo único que la herramienta lee de verdad es una línea:

```text
<!-- pathfinder:execution-mode orchestrator -->
```

Hay dos valores válidos, `human-in-the-loop` y `orchestrator`. Si el archivo no existe, el modo es human-in-the-loop, así que ningún proyecto instalado antes de que existieran los modos necesita migración.

Un archivo con un marcador inválido no se adivina. El ciclo de vida lo dice una vez y continúa en human-in-the-loop. La orquestación se niega a ejecutarse.

Se elige al instalar, o se cambia después:

```bash
npx create-pathfinder --mode orchestrator
```

## Qué recibe realmente un worker

Un *claim* no es una etiqueta. Es un hecho de Git.

Reclamar un ticket crea un worktree enlazado en `.pathfinder/worktrees/<key>` y su propia rama `ticket/<key>-<slug>`. Lo que impide que dos workers sean dueños del mismo ticket son las propias comprobaciones de referencias y worktrees de Git, no un fichero de bloqueo inventado por mí.

Cada worktree lleva su propio `context/current-ticket.md`, así que el estado de un worker nunca puede sobrescribir el de otro. Ese único detalle es la razón por la que el trabajo en paralelo deja de dar miedo.

## El plan que apruebas, una vez

`start` planifica antes de reclamar.

Un ticket es elegible solo si su estado es `Proposed` o `Ready` y todos sus bloqueantes están `Complete`.

Antes de reclamar nada, ves cada ticket seleccionado, su estimación, rol, modelo, esfuerzo y la asignación de worker propuesta. Y apruebas una vez.

Esa aprobación cubre ejecución, claims, commits y pushes en la rama del ticket, PRs en borrador y escrituras en el tracker.

**No** cubre el merge.

El límite de workers es tres por defecto.

## Las estimaciones son evidencia, no promesas

Cada despacho expone la misma forma:

```text
estimate(ticket, claims en vuelo) → perfil de ejecución
select(perfil, política)          → rol, modelo, esfuerzo
dispatch(brief)                   → sesión de worker
```

El perfil registra complejidad, tamaño de contexto, seguridad en paralelo y riesgo, con el motivo de cada uno y si fue derivado o evaluado. La seguridad en paralelo devuelve `isolated`, `shared-surface` o `serialize`.

Tengo cuidado con el peso que le doy a eso. Esas estimaciones salen de las rutas que el propio ticket declara, así que son evidencia gruesa de que dos ramas probablemente no chocarán, no una prueba. El solapamiento real se vuelve a comprobar en la integración.

Esta entrega lleva enrutado **estático**: `developer` implementa, `tester` revisa, y el modelo y el esfuerzo de razonamiento se heredan de la sesión que despacha. El enrutado dinámico de modelos no está incluido. Prefiero entregar la versión aburrida de una capa de enrutado y aprender qué necesita de verdad.

La traducción de harness cubre `claude-code`, `codex` y `manual`. Un valor que el harness no puede cumplir se rechaza en voz alta en vez de degradarse en silencio.

## Las puertas humanas son el punto entero

Cuando un worker se topa con algo que necesita a una persona, registra `State: human-gate` y la pregunta exacta.

Sus dependientes esperan. Todo lo no relacionado sigue.

En GitHub Issues el orquestador añade una etiqueta `gate: human` y un único comentario idempotente, así que un reintento no puede inundar el issue.

El estado de ejecución (`ready`, `working`, `review`, `human-gate`, `done`, `stale`, `failed`, `integrated`) es un vocabulario distinto del ciclo de vida del ticket (`Proposed → Ready → In Progress → Complete`). Confundirlos es cómo una herramienta empieza a decidir cosas en silencio.

## La integración es una decisión aparte

Trabajo terminado no es lo mismo que seguro de mergear.

El integrador coge el trabajo revisado y subido y lo comprueba contra la rama por defecto actual: solapamiento de ficheros modificados, divergencia, evidencia de conflicto con `git merge-tree`, revisión, CI. Devuelve una de tres respuestas.

`candidate`: presenta el merge bajo tu política de aprobación.

`behind`: retoma al developer existente para actualizar sobre la rama por defecto y volver a verificar.

`conflict`: serializa el trabajo en conflicto y se lo devuelve a su developer.

Como un merge cambia la evidencia, cada rama restante se vuelve a comprobar después. Una rama atrasada nunca se disfraza de candidata limpia.

## Sobrevivió a su propia prueba de aceptación

La parte que de verdad me importa no es el diseño. Es que la funcionalidad se construyó con la propia funcionalidad, y que la ejecución falló de una forma útil.

Pathfinder seleccionó dos tickets para ejecutarse a la vez mientras un tercero seguía correctamente bloqueado. Creó claims, ramas y worktrees distintos, y despachó workers de Codex.

Y entonces las dos sesiones alcanzaron sus límites de uso y terminaron.

El trabajo sobrevivió. La rama, el worktree, el claim, el fichero de estado y el perfil de ejecución son todos duraderos, así que sesiones de reemplazo retomaron los mismos claims y siguieron.

Un ticket se integró primero. El orquestador reportó el segundo como `behind`, despachó un brief de rebase y re-verificación contra su worktree preservado, y solo después de una revisión e integración nuevas el grafo de dependencias real hizo elegible al tercero.

Ese fallo no lo planifiqué. Es la mejor evidencia de toda la Feature.

Un límite de cuota, un crash o un harness terminado no borran un ticket:

```text
/orchestrate resume 53.4
```

Un harness compatible incluso puede reemplazar al worker sin reemplazar el ticket. Una sesión de Claude Code con perfil heredado puede ser retomada por Codex en el mismo worktree y la misma rama. No hay failover automático, y no lo quiero. Dos sesiones mutando un mismo worktree es el modo de fallo con el que merece la pena ser paranoico.

## Lo que no es

Ni demonio. Ni broker. Ni cola. Ni base de datos. Ni servidor.

Ni merge automático, ni saltarse la revisión, ni agentes ilimitados.

El motor lee el store de tickets, los bloqueantes, las ramas de Git y el fichero de estado de cada worker cada vez que se ejecuta. No hay nada que arrancar ni nada que mantener vivo. Necesita Node y Git, más `gh` si tus tickets viven en GitHub Issues, mientras que el flujo human-in-the-loop sigue sin necesitar ningún runtime.

## Dónde está ahora mismo

Nueve tickets, nueve pull requests mergeadas en squash, y una ejecución de aceptación con sus capturas y transcripciones registradas en vez de resumidas. Todas las suites en verde: orquestación 166/166, instalador 693/693, renderer 825 pasando con 25 skips documentados.

Ni bump de versión, ni publicación en npm, ni tag, ni release. La capacidad está bajo `[Unreleased]` hasta que decida que merece la pena publicarla, lo cual es una decisión propia y no lo mismo que terminar el trabajo.

Pathfinder ya puede ejecutar varios agentes a la vez. Sigue sin poder aprobar, aceptar, mergear ni publicar nada.

**Pathfinder en GitHub:** https://github.com/rikilamadrid/pathfinder

🧭 **El kit es dueño del flujo de trabajo. El proyecto es dueño del stack.**

<!-- locale:fr -->

Pendant des mois, Pathfinder a fait une chose de manière très délibérée : faire de moi le goulot d'étranglement.

Charger un ticket. Le démarrer. Demander une revue. Accepter le résultat. Le merger. Un à la fois.

Ce n'était jamais un accident. Le kit existe parce qu'un agent de code peut exécuter bien plus vite qu'on ne peut lui faire confiance, et l'écart entre ces deux vitesses est exactement là où s'écrit le mauvais logiciel.

J'ai donc placé un humain à chaque endroit où une décision avait des conséquences.

Ça a marché. C'est aussi devenu lent, pour une raison qui n'avait rien à voir avec le jugement.

## L'attente qui n'était pas la mienne

La file d'attente n'attendait pas que je réfléchisse.

Elle attendait que je sois *disponible*.

Deux tickets touchant des parties totalement différentes d'un code, sans aucune dépendance entre eux, s'exécutaient quand même l'un après l'autre, parce qu'il y avait exactement une session et que j'étais dedans.

Ce n'est pas du *human-in-the-loop*. C'est un humain qui sert d'ordonnanceur.

Pathfinder a donc maintenant deux modes d'exécution sur le même cycle de vie de tickets.

## Une ligne de marqueur

Le mode vit dans un fichier versionné, `context/execution-mode.md`, et la seule chose que l'outil lit réellement tient sur une ligne :

```text
<!-- pathfinder:execution-mode orchestrator -->
```

Il y a deux valeurs valides, `human-in-the-loop` et `orchestrator`. Un fichier absent signifie human-in-the-loop, donc aucun projet installé avant l'existence des modes n'a besoin de migration.

Un fichier avec un marqueur invalide n'est jamais deviné. Le cycle de vie le signale une fois et continue en human-in-the-loop. L'orchestration, elle, refuse de s'exécuter.

On le choisit à l'installation, ou on le change ensuite :

```bash
npx create-pathfinder --mode orchestrator
```

## Ce qu'un worker reçoit vraiment

Un *claim* n'est pas une étiquette. C'est un fait Git.

Réclamer un ticket crée un worktree lié dans `.pathfinder/worktrees/<key>` et sa propre branche `ticket/<key>-<slug>`. Ce qui empêche deux workers de posséder le même ticket, ce sont les vérifications de référence et de worktree de Git lui-même, pas un fichier de verrou que j'aurais inventé.

Chaque worktree porte son propre `context/current-ticket.md`, donc l'état d'un worker ne peut jamais écraser celui d'un autre. Ce seul détail est la raison pour laquelle le travail en parallèle cesse d'être effrayant.

## Le plan que vous approuvez, une fois

`start` planifie avant de réclamer.

Un ticket n'est éligible que si son statut est `Proposed` ou `Ready` et que chaque bloqueur est `Complete`.

Avant le moindre claim, vous voyez chaque ticket sélectionné, son estimation, son rôle, son modèle, son effort et l'affectation de worker proposée. Puis vous approuvez une fois.

Cette approbation couvre l'exécution, les claims, les commits et pushes sur la branche du ticket, les PR en brouillon et les écritures dans le tracker.

Elle ne couvre **pas** le merge.

La limite de workers est de trois par défaut.

## Les estimations sont des indices, pas des promesses

Chaque dispatch expose la même forme :

```text
estimate(ticket, claims en vol) → profil d'exécution
select(profil, politique)       → rôle, modèle, effort
dispatch(brief)                 → session de worker
```

Le profil enregistre la complexité, la taille de contexte, la sûreté en parallèle et le risque, avec la raison de chacun et s'il a été dérivé ou évalué. La sûreté en parallèle renvoie `isolated`, `shared-surface` ou `serialize`.

Je fais attention au poids que je donne à cela. Ces estimations viennent des chemins que le ticket déclare lui-même, donc ce sont des indices grossiers que deux branches n'entreront probablement pas en collision, pas une preuve. Le chevauchement réel est revérifié à l'intégration.

Cette version embarque un routage **statique** : `developer` implémente, `tester` relit, et le modèle et l'effort de raisonnement sont hérités de la session qui dispatche. Le routage dynamique de modèles n'y est pas. Je préfère livrer la version ennuyeuse d'une couche de routage et apprendre ce dont elle a réellement besoin.

La traduction de harness couvre `claude-code`, `codex` et `manual`. Une valeur que le harness ne peut pas honorer est refusée explicitement plutôt que dégradée en silence.

## Les portes humaines sont tout l'enjeu

Quand un worker tombe sur quelque chose qui nécessite une personne, il enregistre `State: human-gate` et la question exacte.

Ses dépendants attendent. Tout ce qui n'a aucun rapport continue.

Sur GitHub Issues, l'orchestrateur ajoute un label `gate: human` et un unique commentaire idempotent, de sorte qu'une nouvelle tentative ne peut pas inonder l'issue.

L'état d'exécution (`ready`, `working`, `review`, `human-gate`, `done`, `stale`, `failed`, `integrated`) est un vocabulaire distinct du cycle de vie du ticket (`Proposed → Ready → In Progress → Complete`). Les confondre, c'est ainsi qu'un outil commence à décider en silence.

## L'intégration est une décision à part

Travail terminé n'est pas la même chose que sûr à merger.

L'intégrateur prend le travail relu et poussé et le confronte à la branche par défaut actuelle : chevauchement de fichiers modifiés, divergence, preuve de conflit via `git merge-tree`, revue, CI. Il renvoie l'une de trois réponses.

`candidate` : présentez le merge selon votre politique d'approbation.

`behind` : relancez le developer existant pour se remettre à jour sur la branche par défaut et revérifier.

`conflict` : sérialisez le travail en conflit et rendez-le à son developer.

Comme un merge change les preuves, chaque branche restante est revérifiée ensuite. Une branche en retard n'est jamais déguisée en candidate propre.

## Il a survécu à sa propre recette

La partie qui compte vraiment pour moi n'est pas la conception. C'est que la fonctionnalité a été construite par la fonctionnalité, et que l'exécution a mal tourné d'une manière utile.

Pathfinder a sélectionné deux tickets à exécuter en parallèle pendant qu'un troisième restait correctement bloqué. Il a créé des claims, des branches et des worktrees distincts, et dispatché des workers Codex.

Puis les deux sessions ont atteint leurs limites d'usage et se sont arrêtées.

Le travail a survécu. La branche, le worktree, le claim, le fichier d'état et le profil d'exécution sont tous durables, donc des sessions de remplacement ont repris les mêmes claims et continué.

Un ticket s'est intégré en premier. L'orchestrateur a signalé le second comme `behind`, dispatché un brief de rebase et de revérification contre son worktree préservé, et ce n'est qu'après une nouvelle revue et intégration que le vrai graphe de dépendances a rendu le troisième éligible.

Je n'avais pas prévu cette panne. C'est la meilleure preuve de toute la Feature.

Une limite de quota, un crash ou un harness terminé n'effacent pas un ticket :

```text
/orchestrate resume 53.4
```

Un harness compatible peut même remplacer le worker sans remplacer le ticket. Une session Claude Code au profil hérité peut être reprise par Codex dans le même worktree et la même branche. Il n'y a pas de bascule automatique, et je n'en veux pas. Deux sessions qui modifient un même worktree, c'est le mode de défaillance qui mérite qu'on soit paranoïaque.

## Ce que ce n'est pas

Pas de démon. Pas de broker. Pas de file. Pas de base de données. Pas de serveur.

Pas de merge automatique, pas de contournement de revue, pas d'agents illimités.

Le moteur lit le store de tickets, les bloqueurs, les branches Git et le fichier d'état de chaque worker à chaque exécution. Il n'y a rien à démarrer et rien à maintenir en vie. Il nécessite Node et Git, plus `gh` si vos tickets vivent dans GitHub Issues, tandis que le flux human-in-the-loop ne nécessite toujours aucun runtime.

## Où ça en est aujourd'hui

Neuf tickets, neuf pull requests mergées en squash, et une recette d'acceptation dont les instantanés et les transcriptions sont enregistrés plutôt que résumés. Toutes les suites au vert : orchestration 166/166, installeur 693/693, renderer 825 passants avec 25 skips documentés.

Aucun bump de version, aucune publication npm, aucun tag, aucune release. La capacité reste sous `[Unreleased]` jusqu'à ce que je décide qu'elle mérite d'être publiée, ce qui est une décision à part entière et pas la même chose que terminer le travail.

Pathfinder peut désormais faire tourner plusieurs agents à la fois. Il ne peut toujours ni approuver, ni accepter, ni merger, ni publier quoi que ce soit.

**Pathfinder sur GitHub :** https://github.com/rikilamadrid/pathfinder

🧭 **Le kit possède le workflow. Le projet possède la stack.**
