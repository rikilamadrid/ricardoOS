# Pin this project to the public npm registry, regardless of any global
# ~/.npmrc that points at a private feed. Keeps installs reproducible on CI.
registry=https://registry.npmjs.org/
always-auth=false
